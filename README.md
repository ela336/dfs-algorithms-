Depth-First Search (DFS) in C++
This  program implements DFS (Depth-First Search) on a 2D grid. It explores all reachable cells starting from a given starting position.

another C++ program implements Depth-First Search (DFS) on an undirected graph and counts the number of connected components.
